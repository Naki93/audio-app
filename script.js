// window.addEventListener("load", function () {
//   const image = document.getElementById("parallax-image");

//   // Simulate the parallax movement by shifting the image
//   let scrollAmount = -200; // How much the image moves (adjust as necessary)

//   // Create the movement effect
//   const parallaxEffect = setInterval(() => {
//     image.style.transform = `translateY(${scrollAmount}px)`;
//     scrollAmount += 5; // Adjust movement speed
//     if (scrollAmount >= 0) {
//       clearInterval(parallaxEffect); // Stop the movement when it reaches the desired position
//     }
//   }, 30); // Speed of the movement (adjust as necessary)
// });
